# Backend local Traffic Monitor (Bai 9)
#   pip install -r requirements.txt
#
# GPU NVIDIA: cai torch ban CUDA TRUOC (neu chua co), vi du CUDA 12.x:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Kiem tra:  python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"

ultralytics>=8.3
supervision>=0.26
opencv-python>=4.9
numpy
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart
